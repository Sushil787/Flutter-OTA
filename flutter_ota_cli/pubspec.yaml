name: flutter_ota_cli
description: Flutter OTA CLI — build the app, cut releases, create patches, and upload artifacts to the backend.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.2

executables:
  flutter_ota: flutter_ota

dependencies:
  args: ^2.5.0
  http: ^1.2.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
