name: flutroid_cli
description: Flutroid OTA CLI — build the app, cut releases, create patches, and upload artifacts to the backend.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.2

executables:
  flutroid: flutroid

dependencies:
  args: ^2.5.0
  http: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
